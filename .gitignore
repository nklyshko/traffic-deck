# External reference material — not part of this repo's history
/_EXTERNAL/

# Generated gRPC stubs are committed (regenerate with `make gen` after editing proto/).

# Local object-store data (plan §6.2)
/data/

# Go build artifacts
/gateway/gateway
/trafficdeck

# Python
__pycache__/
*.pyc
.venv/
/tui/.venv/
/mcp/.venv/

# mise local overrides
mise.local.toml

# Local Claude Code settings
/.claude/settings.local.json

# The plan is maintained as its own git repository
/plan/
