version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # We use domain-shaped message names (Flow, Bookmark, Group, CaptureChunk,
    # FlowBatch) and reuse Empty, rather than synthesising a *Request/*Response
    # per RPC (plan §5).
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
