[project]
name = "capture-tools"
version = "0.0.0"
description = "Capture agents (Chrome, mitmproxy, Android) for the traffic analysis system"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.76",
    "grpcio-tools>=1.76",
    "protobuf>=5.29",
    "mitmproxy>=11",
    "frida>=16",
]

[tool.uv]
package = false
